# CLAUDE.md

**Read `AGENTS.md` — it is the canonical instruction file for every AI agent in this repository.**

This file is a pointer, deliberately. Two full copies of the same conventions drift apart within a
month, and nothing detects it because both look maintained.

Put **only** Claude-specific mechanics here — a hook this repo installs, a skill it ships, a tool
permission it needs. Anything that would be true for another harness belongs in `AGENTS.md`.
