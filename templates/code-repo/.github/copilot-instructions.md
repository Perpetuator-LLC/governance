# Copilot instructions

**Read `AGENTS.md`** — it is the canonical instruction file for every AI agent in this
repository, whatever harness you are. This file is a pointer so the surfaces cannot drift apart.

Put only Copilot-specific mechanics here; anything true for another harness belongs in `AGENTS.md`.
