# The plain env file this service starts from: module settings only, NO core input (identity issuer,
# secret store, edge network, bus). Never a secret: a value that would burn on disclosure belongs in
# the secret store, bound by the overlay.
APP_IMAGE=example/app:1.0.0
APP_PORT=8080
